/** Homepage carousel shows newest N; view-all page uses ALL_LIMIT. */
const NEW_ARRIVAL_HOME_LIMIT = 5;
const NEW_ARRIVAL_ALL_LIMIT = 100;

module.exports = {
  NEW_ARRIVAL_HOME_LIMIT,
  NEW_ARRIVAL_ALL_LIMIT,
};
